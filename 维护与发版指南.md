# AdbTools 维护 / 发版 / 推送指南

> 本文件供未来的 AI 助手或维护者快速恢复上下文。**新对话开始后，请先读本文件**，即可了解如何改代码、编译、发版、推送。
> 本文件不含任何密钥（token），token 需要每次使用时由用户提供。

---

## 一、项目概况

- **软件名**：ADB 文件浏览器（AdbTools）
- **功能**：Android 设备文件管理、上传/下载、截图、安装 APK、应用管理、实时 logcat、scrcpy 投屏、adb/scrcpy/自身版本检测与一键更新
- **技术栈**：C++17 + [EUI-NEO](https://github.com/sudoevolve/EUI-NEO) UI 框架（**vendored** 在 `3rd/EUI-NEO/`，含本项目定制修改）+ MinGW-w64 + CMake
- **开源协议**：Apache-2.0
- **工作区路径**：`C:\Users\green\AdbTools`
- **GitHub 仓库**：`https://github.com/joffeego/AdbTools`（owner = `joffeego`，主分支 = `main`）

---

## 二、本地环境关键事实

- **编译工具链**：MinGW-w64（GCC 12+）。本机用 WinLibs（winget 安装，路径含 `BrechtSanders.WinLibs.POSIX.UCRT...`）
- **CMake 生成器**：`MinGW Makefiles`
- **`gh` CLI 未安装**；**没有 SSH key** → **推送只能用 HTTPS + Personal Access Token（PAT）**
- **git 用户**（本仓库已配置）：`joffeego` / `joffeego@users.noreply.github.com`
- 编译产物：`build/adb_browser.exe`
- 第三方二进制（scrcpy.exe、adb.exe、DLL）**不入库**，由 `scripts/fetch_scrcpy.ps1` 或 CI 下载

---

## 三、改代码后的编译方法

```powershell
# 先杀掉正在运行的 exe，否则重新链接会报 "Permission denied"
Get-Process -Name adb_browser -ErrorAction SilentlyContinue | Stop-Process -Force
Start-Sleep -Milliseconds 300

cmake --build build --parallel
```

> 全新配置（一般不需要，除非 build 目录坏了）：
> `cmake -S . -B build -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release`

**冒烟测试**：
```powershell
$p = Start-Process -FilePath .\build\adb_browser.exe -PassThru
Start-Sleep -Seconds 3
$p.Refresh()
"alive=$(-not $p.HasExited)  MainWindowHandle=$($p.MainWindowHandle)"
if (-not $p.HasExited) { Stop-Process -Name adb_browser -Force; "killed" }
```

---

## 四、版本号位置（发版前必须改）

| 文件 | 位置 | 内容 |
| --- | --- | --- |
| `main.cpp` | 顶部常量 | `constexpr const char* kAppVersion = "0.9.6";` |
| `installer.iss` | 顶部 `#ifndef` 块 | `#define MyAppVersion "0.9.6"` |

- 自更新仓库地址：`main.cpp` 里 `kAppUpdateRepo = "joffeego/AdbTools"`（一般不动）
- tag 命名规则：`vX.Y.Z`（小写 v 前缀）

---

## 五、发版 + 推送完整流程（一步步照做）

### 1. 改版本号（见上）

### 2. 本地编译验证

```powershell
Get-Process -Name adb_browser -ErrorAction SilentlyContinue | Stop-Process -Force
Start-Sleep -Milliseconds 300
cmake --build build --parallel
```

### 3. 提交并推送 main

```powershell
git add -A
git commit -m "描述这次改动"
git push https://joffeego:<TOKEN>@github.com/joffeego/AdbTools.git main
```

### 4. 打 tag 触发 CI 自动发版

```powershell
git tag v0.9.6
git push https://joffeego:<TOKEN>@github.com/joffeego/AdbTools.git v0.9.6
```

### 5. 如果 tag 打错了 / CI 失败了，移动 tag 重跑

```powershell
# 先修复并提交，然后：
git tag -f v0.9.6
git push -f https://joffeego:<TOKEN>@github.com/joffeego/AdbTools.git v0.9.6
```

---

## 六、推送需要什么（权限 / token）

- 需要 **Personal Access Token (classic)**，scope 必须勾：
  - ✅ `repo`（推送代码）
  - ✅ `workflow`（推送 `.github/workflows/*.yml`）
- 生成地址：https://github.com/settings/tokens/new
- token 格式：`ghp_...` 开头
- 让用户提供 token 后，用它替换上面命令里的 `<TOKEN>`
- **安全**：token 只用于当次推送命令（不写入 `.git/config`）；用完建议让用户到 token 页面 Revoke

> 仓库已经 `git init` 过、`main` 分支已 `--set-upstream-to=origin/main`，所以不需要重新 init。

---

## 七、CI 自动构建说明（.github/workflows/release.yml）

- **触发**：push 以 `v` 开头的 tag，或手动 workflow_dispatch
- **流程**：
  1. MSYS2 装 MinGW-w64（gcc/cmake/make）→ CMake 配置 → 编译
  2. `scripts/fetch_scrcpy.ps1` 下载 scrcpy+adb 到 `build/scrcpy`
  3. `choco install innosetup` → `iscc installer.iss /DMyAppVersion=...` 打安装包
  4. 打包便携版 zip → 上传 Release（zip + Setup.exe）
- **产物**：`AdbFileBrowser-windows-x64.zip`（便携）+ `AdbTools-Setup-<版本>.exe`（安装版，中文向导）
- **安装版说明**：装到 `%LOCALAPPDATA%\Programs\AdbTools`（用户目录，无需管理员），带开始菜单/桌面快捷方式、卸载程序

### 验证 CI 结果（用 GitHub API）

```powershell
$headers = @{ "Authorization" = "Bearer <TOKEN>"; "User-Agent" = "AdbTools-deploy" }
# 查看最近的 workflow run 状态
$runs = Invoke-RestMethod -Uri "https://api.github.com/repos/joffeego/AdbTools/actions/runs?per_page=1" -Headers $headers
$runs.workflow_runs | ForEach-Object { "status=$($_.status) conclusion=$($_.conclusion)" }
# 查看最新 release 资产
$rel = Invoke-RestMethod -Uri "https://api.github.com/repos/joffeego/AdbTools/releases/latest" -Headers $headers
$rel.assets | ForEach-Object { "  - $($_.name)" }
```

---

## 八、关键文件清单

| 文件/目录 | 说明 |
| --- | --- |
| `main.cpp` | 应用全部逻辑（~6000 行） |
| `regex_wrap.cpp` | std::regex 异常隔离封装（单独开 `-fexceptions` 编译） |
| `CMakeLists.txt` | 构建脚本（`eui_neo_configure_app` + winhttp + regex_wrap 的 exceptions） |
| `installer.iss` | Inno Setup 安装脚本（中文向导） |
| `ChineseSimplified.isl` | 安装向导中文语言文件 |
| `scripts/fetch_scrcpy.ps1` | 下载 scrcpy（含 adb） |
| `.github/workflows/release.yml` | CI 自动发版 |
| `3rd/EUI-NEO/` | 框架源码（vendored，已去掉其内部 `.git`，按普通文件提交） |
| `screenshots/` | README 截图 |

**`.gitignore` 已排除**：`build/`、`dist/`、`*.exe/*.dll`、scrcpy 二进制、`adb_file_browser_*.txt`（运行时状态/设置）、`.adb_preview_*`、`adb_browser_edit_tmp.txt`。

---

## 九、代码里几个重要机制（改代码前必读）

1. **窗口尺寸是「逻辑像素」**，用 `windowDpiScale() × settings.uiScale` 换算成物理像素；`restoreWindowState` / `saveWindowState` 存的是逻辑尺寸。
2. **最小窗口尺寸**：`applyMinWindowSize()`（880×420 逻辑），在启动和 `selectUiScale` 时调用。
3. **自更新**：`installAppUpdateWorker` 下载 zip → 解压 → 把运行中的 exe 改名 `.old` → 复制新 exe → 顺带更新 assets/scrcpy。下载用 `downloadFileWithFallback`（直连 GitHub 失败后走 ghfast.top 等镜像，因为国内直连 github.com 会被墙）。
4. **logcat 流**：后台线程 `streamLogcatWorker` 跑 `adb logcat` 持续读管道；过滤是大小写不敏感正则（`regex_wrap.cpp` 提供异常隔离）。
5. **设置/书签/命令/最近路径**：都存到 `executableDir()`（exe 所在目录），不是 CWD，避免「换启动方式导致设置丢失」。
6. **字体图集**：`3rd/EUI-NEO/core/render/text.cpp` 里 glyph atlas 满了会自动 `resetAtlasPage` 重新打包（`textAtlasResetCounter`），避免字形永久消失。
7. **图标字体**：Font Awesome 7 **Free Solid**（`assets/Font Awesome 7 Free-Solid-900.otf`）。**注意不要用 brands 图标**（如 0xF17B android 在 Free Solid 里没有，会导致图标消失）；要用 solid 图标码（如 0xF50D box-open、0xF15C file-lines）。

---

## 十、常用 git 命令速查

```powershell
git status -sb                     # 看状态 + 上游
git log --oneline -5               # 最近提交
git ls-remote origin               # 看远端分支/提交
git remote -v                      # 看远端地址（应为 https://github.com/joffeego/AdbTools.git）
```

---

## 十一、以前踩过的坑（重要）

1. `3rd/EUI-NEO` 原本自带 `.git`（内嵌仓库）→ 已删除并转为普通文件提交，**不要再 `git submodule` 或重新 clone 它的 .git**。
2. 图标用 brands 码（0xF17B）→ 图标消失 → 改用 solid 码。
3. 设置存 CWD → 换启动方式后设置被重置 → 已改为存 exe 目录。
4. 国内直连 github.com/release-assets 被墙 → 下载失败 → 已加镜像回退（ghfast.top 等）。
5. 字体图集满了丢字形 → 已加「清空重打包」。
6. Inno Setup 中文语言文件 choco 里没有 → 已提交 `ChineseSimplified.isl` 到仓库。
7. `installer.iss` 的 AppId 用 `{{GUID}}`（双花括号转义），写错会导致 iscc 编译失败。
